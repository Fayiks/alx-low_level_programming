#include <stdio.h>
/**
 * main -Function begins
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int d;

	while (d = 97; d < 123; d++)
	{
		if (d != 101 && d != 113)
		{
			putchar(d);
		}
	}
	putchar('\n');
	return (0);
}
